application:
    build: .
    ports:
        - "8000:8000"
    volumes:
        - ./php:/usr/src/php
    links:
        - mysql:mysql

mysql:
  image: mysql
  ports:
    - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: password
    MYSQL_DATABASE: typo3
    MYSQL_USER: root
